version: '3'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    expose:
      - 8000
  nginx:
    image: nginx:latest
    ports:
      - 8080:8080
    volumes:
      - ./nginx_config.conf:/etc/nginx/conf.d/default.conf
    depends_on: 
      - backend
  